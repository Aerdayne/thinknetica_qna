$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');
$(".alert").remove();
$(".col-lg-9").prepend('<%= bootstrap_flash %>');
<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>');
  $('.answer-new #answer_body').val('');
<% end %>